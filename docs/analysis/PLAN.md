---
run: 2026-08-31
commit: 2a92289a6e8e79e11486c9e73301b138212a3e8c
tasks: 11
phases: 4
---

# Remediation plan

Generated by /analizeMax. Every task here passed the admission test: reversible,
verifiable, no behaviour change, no judgement call, bounded.
Execute with `/analizeMax-execute`.

Derived from `2026-08-31-1411-full.md`. Task IDs match the finding IDs in that
report. Phases run in order — each ends in a state that builds, gates and
passes invariants, so an interrupted run leaves the tree better rather than
half-changed.

---

## Phase 1 — Factual corrections to docs, rules and comments

Zero build risk: no file in this phase is compiled or executed.

**Verify:** `pwsh -NoProfile -File tools/check-invariants.ps1`

### D3 — Correct both `rhi-d3d12` dependency statements in ARCHITECTURE.md
- **Files:** `docs/ARCHITECTURE.md`
- **Why:** The prose says "`rhi-d3d12` links `rhi`, and nothing else" and the
  graph block shows `rhi-d3d12 → rhi`, but
  `packages/rhi-d3d12/CMakeLists.txt:7` declares
  `PUBLIC_DEPS engine::rhi engine::math`, and the edge is real —
  `device_d3d12.cpp:5` includes `<engine/math/mip.hpp>` and `:2098` calls
  `math::build_rgba8_mip_chain`. The `dependency-direction` invariant cannot
  catch this: `math` is Layer 0, so the edge is legally downward.
- **Do:** In the ASCII dependency block, change the Layer 2 entry
  `rhi-d3d12  → rhi` to `rhi-d3d12  → rhi, math`. In the prose two paragraphs
  below, replace "`rhi-d3d12` links `rhi`, and nothing else." with
  "`rhi-d3d12` links `rhi` and `math` — the latter only for
  `build_rgba8_mip_chain`." Leave the preceding sentence about `rhi` itself
  ("depends on `core` only — **not** `math`") unchanged; it is correct.
- **Proof:** `grep -n "rhi-d3d12" docs/ARCHITECTURE.md` shows `math` in both
  places, and
  `grep -n "PUBLIC_DEPS" packages/rhi-d3d12/CMakeLists.txt` still shows
  `engine::rhi engine::math`. Then `doc-links` and all 13 invariants pass.

### T1 — Add the missing thirteenth check to CLAUDE.md's enumeration
- **Files:** `CLAUDE.md`
- **Why:** `CLAUDE.md:98` says "Thirteen checks:" and then names twelve —
  `map-dependencies` is absent. Line 104 then says "The last one reads
  ENGINE_MAP.md as a graph", but the last one enumerated is the CMake `if()`
  check. This is the first file every session reads, describing the tool every
  session runs before shipping.
- **Do:** Insert the map check into the enumerated list — after "spec statuses,"
  add "the ENGINE_MAP dependency graph," — and change "The last one reads
  ENGINE_MAP.md as a graph:" to "That last one reads ENGINE_MAP.md as a graph:"
  so the sentence attaches to the item just added rather than to the `if()`
  check. Keep the count word "Thirteen"; it is correct.
- **Proof:** The enumeration in `CLAUDE.md` names thirteen items, matching the
  thirteen `Add-Result` names emitted by
  `pwsh -NoProfile -File tools/check-invariants.ps1`. Compare against
  `grep -c "^Add-Result" tools/check-invariants.ps1` → `13`.

### T2 — Complete the global-state exception list in cpp-conventions.md
- **Files:** `.claude/rules/cpp-conventions.md`
- **Why:** The rule states "Three deliberate exceptions" to no-hidden-global-
  state and names the logger, the cvar registry, and `warn_physics_capacity`'s
  latch. The tree holds more, all deliberate and none listed:
  `packages/core/src/profile.cpp:131-132` (`g_profiler`, `g_frame_profiler`,
  set via `set_profiler()` — structurally identical to the logger exception the
  rule does name), `profile.cpp:98` (a `static bool warned` latch whose own
  comment cross-references `warn_physics_capacity`), and
  `log_file.cpp:101-102` (`static std::unique_ptr<ILogger> installed;` and
  `static bool attempted`). An absolute count with unlisted exceptions makes a
  session re-derive whether each one is a violation.
- **Do:** Change "Three deliberate exceptions" to "Five deliberate exceptions"
  and add two entries to the list, in the same style as the existing three:
  the profiler pair in `core/src/profile.cpp` (set once at startup via
  `set_profiler()`, mirroring the logger), and `install_file_logger`'s
  function-local statics in `core/src/log_file.cpp` (install-once, so the sink
  outlives every caller). Fold the `profile.cpp:98` latch into the existing
  latch bullet by naming both sites, since it is the same pattern.
- **Proof:** Every symbol matched by
  `grep -rnE '^(static |)[A-Za-z_][A-Za-z0-9_:<>*& ]*\s+g_[a-z_]+' packages/ --include='*.cpp' | grep -v third_party`
  plus the four mutable function-local statics in `core` appears in the rule's
  list, and the stated count equals the number of bullets.

### T4 — Correct the assert-site counts in two places
- **Files:** `packages/core/src/assert.cpp`, `docs/ROADMAP.md`
- **Why:** Both say 77 sites; the measured figure is **76** invocation sites —
  **56** bare `ENGINE_ASSERT(` and **20** `ENGINE_ASSERT_MSG(`.
  `docs/ROADMAP.md:1105` additionally says "~40 bare", which is off by 40%. The
  ROADMAP entry is stamped "Last updated: 31 Aug 2026", so these were
  miscounted rather than outdated. Neither correction changes the argument the
  numbers support.
- **Do:** In `assert.cpp:19`, change "the 77 assert sites" to "the 76 assert
  sites". In `ROADMAP.md:1105`, change "the ~40 bare `ENGINE_ASSERT` sites of
  77" to "the 56 bare `ENGINE_ASSERT` sites of 76". Change nothing else in
  either sentence.
- **Proof:**
  ```bash
  grep -rn "ENGINE_ASSERT" packages/ --include='*.cpp' --include='*.hpp' \
    | grep -v 'core/include/engine/core/assert.hpp' | grep -vE ':\s*//' \
    | grep -E 'ENGINE_ASSERT(_MSG)?\(' | wc -l
  ```
  prints `76`; the same pipeline filtered to `ENGINE_ASSERT(` prints `56`. No
  occurrence of `77` remains in either file's assert prose.

### D5 — Renumber the three misnumbered invariant section comments
- **Files:** `tools/check-invariants.ps1`
- **Why:** The numbered comment headers disagree with execution order for the
  last three checks. `map-dependencies` is 11th in output but labelled
  `# ── 13.`; `analysis-set` is 12th but labelled `# ── 11.`;
  `conditional-target-links` is 13th but labelled `# ── 12.`. Checks 1–10 are
  correct. A reader navigating by number is misdirected.
- **Do:** At line 293 change `# ── 13.` to `# ── 11.`; at line 417 change
  `# ── 11.` to `# ── 12.`; at line 547 change `# ── 12.` to `# ── 13.`. Change
  no code and no prose after the number.
- **Proof:** `grep -n "^# ── [0-9]*\." tools/check-invariants.ps1` lists 1–13 in
  ascending order, matching the order of
  `grep -n "^Add-Result" tools/check-invariants.ps1`. All 13 checks still pass.

### D4 — Add PowerShell 7 to README's Requirements list
- **Files:** `README.md`
- **Why:** README and CLAUDE.md both document
  `pwsh -NoProfile -File tools/check-invariants.ps1`, and `pwsh` is not part of
  a base Windows 11 install — the shipped shell is Windows PowerShell 5.1
  (`powershell`). The Requirements list names Windows, a D3D12 GPU, Visual
  Studio 2026, the Windows SDK, CMake 4.2+, C++20 and Git, but not PowerShell 7.
  A new contributor hits `pwsh : not recognized` on a documented command.
- **Do:** Add one bullet to the Requirements list, in the same style as the
  others (name, link, and why): PowerShell 7 (`pwsh`), needed by
  `tools/check-invariants.ps1` as documented. Note in the same bullet that the
  script also runs unchanged under the built-in Windows PowerShell 5.1
  (`powershell -NoProfile -File tools/check-invariants.ps1`) — verified during
  this audit: exit 0, all 13 checks pass.
- **Proof:** `powershell -NoProfile -File tools/check-invariants.ps1` exits 0
  with "all 13 checks passed", and the Requirements list names PowerShell. The
  `doc-links` invariant still passes with any added link.

---

## Phase 2 — Dead files and one dead link dependency

Touches content and the build graph, so this phase rebuilds.

**Verify:**
```powershell
cmake --build build --config Debug; cmake --build build --config Release --target game; .\build\bin\Debug\sandbox.exe --gates; .\build\bin\Release\game.exe --gates; pwsh -NoProfile -File tools/check-invariants.ps1
```

### D2 — Delete three unreferenced content files
- **Files:** `packages/sandbox/content/meshes/cartoon_husky.obj` (872,148 B),
  `packages/sandbox/content/meshes/cartoon_husky.mtl` (363 B),
  `packages/sandbox/content/shaders/triangle.hlsl` (372 B)
- **Why:** Zero references anywhere. Established by enumerating every content
  path string in `packages/sandbox/src/main.cpp` (36 of them — none names these
  files), the cooker's hard-coded five-asset list
  (`packages/cook/src/main.cpp:130-138`), every CMake `DEPENDS` entry, and every
  markdown file. `cartoon_husky.obj` is a leftover from before the glTF
  migration; README already states "The husky mesh is glTF…; the cube stays
  OBJ." They are not inert: `engine_copy_install_content` runs `copy_directory`
  over the whole content tree, so all three are copied next to `sandbox.exe`
  *and* `game.exe` on every build, and `packages/game/CMakeLists.txt` installs
  the directory wholesale into the player layout. The 872 KB is 15% of the
  repository's entire 5.7 MB tracked size, in a public repo.
- **Do:** `git rm` the three files. Change nothing else — no CMake edit is
  needed, because nothing references them.
- **Proof:** After a Debug and a Release `game` rebuild, all 71 gates pass in
  both binaries and
  `ls build/bin/Release/content/meshes/` lists only `cartoon_husky.bin`,
  `cartoon_husky.gltf` and `cube.obj`, and
  `ls build/bin/Release/content/shaders/` no longer lists `triangle.hlsl`. The
  `glTF gate`, `Husky mesh gate` and `Cook gate` still report
  `verts=7132 indices=37506` unchanged.

### P5 — Remove the unused `d3dcompiler` link dependency
- **Files:** `packages/rhi-d3d12/CMakeLists.txt`
- **Why:** Line 8 declares `PRIVATE_DEPS d3d12 dxgi d3dcompiler`, and no source
  in the tree includes `d3dcompiler.h` or calls `D3DCompile*` — shaders are
  compiled through DXC (`dxcompiler`, linked by `shaders-dxc`). The CMake entry
  is the *only* occurrence of the string `d3dcompiler` in the entire repository.
  A dead link dependency on a legacy Windows redistributable, and noise in the
  link list any portability review has to read past.
- **Do:** Change `PRIVATE_DEPS d3d12 dxgi d3dcompiler` to
  `PRIVATE_DEPS d3d12 dxgi`.
- **Proof:** `cmake -B build -G "Visual Studio 18 2026" -A x64` regenerates and
  both configurations link. `grep -rn "d3dcompiler\|D3DCompile" packages/` finds
  nothing. All 71 gates pass in Debug and Release, including
  `DXC gate: profile=vs_6_0 target=dxil dxil=yes bytes=6444` and
  `Shader cache gate: first=hit second=hit dxil=yes`, which are what would break
  if shader compilation had in fact depended on it.

---

## Phase 3 — Two code corrections

**Verify:**
```powershell
cmake --build build --config Debug; .\build\bin\Debug\sandbox.exe --gates; $env:ENGINE_GPU_DEBUG=1; .\build\bin\Debug\sandbox.exe --gates; $env:ENGINE_GPU_DEBUG=$null; cmake --build build --config Release --target game; .\build\bin\Release\game.exe --gates
```

### S3 — Stop one failing gate from hiding the next
- **Files:** `packages/sandbox/src/main.cpp`
- **Why:** Line 5752 reads
  `gates_ok = run_graph_gate() && run_swap_gate() && gates_ok;`. `&&`
  short-circuits, so if `run_graph_gate()` returns false, `run_swap_gate()` is
  never called and a red run reports one failure when there are two. The
  surrounding ~25 call sites (`main.cpp:5680-5749`) all use the pattern that
  always runs, so there is exactly one correct answer and it is already
  established in this file. This degrades failure diagnosis, which is the
  project's core method.
- **Do:** Replace line 5752 with two statements in the file's dominant form:
  ```cpp
  if (!run_graph_gate()) {
      gates_ok = false;
  }
  if (!run_swap_gate()) {
      gates_ok = false;
  }
  ```
  Leave line 5753-5755 (`run_frame_ring_budget_gate`) alone — it already
  evaluates its call before the `&&`, so it always runs.
- **Proof:** `--gates` still exits 0 and still logs both
  `Graph gate missing-producer (pass)` / `Graph gate copy-format (pass)` /
  `Graph gate valid-copy (pass)` / `Graph gate cycle (pass)` and
  `Swap gate: standard frame owned by renderer (pass)`. The total stays at 71
  `(pass)` lines with 0 `FAIL` in both Debug and Release — no behaviour change
  on a green tree, which is what makes this safe.

### S4 — Reject a pass that declares more refs than the graph can hold
- **Files:** `packages/renderer/src/render_graph.cpp`
- **Why:** `RenderPassDesc::kMaxRefs = 4` with `ResourceRef writes[4]` /
  `reads[4]` and caller-set `write_count` / `read_count`. `add_pass`
  (`render_graph.cpp:541`) only pushes the desc; `compile()` then iterates
  `for (u32 i = 0; i < pass.read_count; ++i)` at `:62` and `:637`, so a pass
  author who sets `read_count = 5` reads past a fixed array. Highest count
  actually in use is 3 (`standard_frame.cpp:185`, `:200`). This is a missing
  guard behind an existing pattern: `compile()` already logs and rejects every
  other invalid desc, naming the pass each time.
- **Do:** In `add_pass`, before `passes_.push_back`, clamp both counts to
  `RenderPassDesc::kMaxRefs` and log an error naming the pass and the rejected
  count, in the same voice as the existing graph messages (e.g.
  `"Graph: pass '<name>' declares <n> reads, capped at 4"`). Clamp rather than
  drop the pass, so the failure is loud but the frame still renders.
- **Proof:** `Graph compiled: 25 passes, 19 resources` is unchanged and no new
  log line appears on a normal run, because nothing in the tree exceeds 3.
  All 71 gates pass, including the four negative graph gates. With
  `ENGINE_GPU_DEBUG=1` the debug layer still reports
  `0 message(s), 0 error(s), 0 warning(s)`.

---

## Phase 4 — Close the one CI gap this audit found

**Verify:** `pwsh -NoProfile -File tools/check-invariants.ps1` — and prove the
new check actually bites by temporarily changing one number in ENGINE_MAP.md,
confirming it goes red, then reverting.

### D7 — Reconcile ENGINE_MAP's 22 hand-written counts against a recount
- **Files:** `tools/check-invariants.ps1`
- **Why:** `docs/ENGINE_MAP.md` carries 22 hand-maintained numbers — the header
  line `76 Done · 43 Ready · 102 Later · 60 Far` and 21 per-category
  `*N done · M ready · K rows.*` subtotals. I recounted all of them for this
  audit and they are **correct today**, but nothing verifies them. The
  `map-dependencies` check already parses every row and already computes
  `$ready` for its own summary line — it simply never compares its count
  against the header. This is exactly the reconciliation `roadmap-audit`
  performs for the ROADMAP's LOC figures, not performed here.
- **Do:** Extend the existing `map-dependencies` block (it already builds
  `$rowStatus`, keyed `"$cat/$row"`, and `$catNames`). Track per-category
  Done/Ready/row tallies while parsing. After the loop, add two violation
  checks: (1) parse the header line matching
  `^(\d+) Done · (\d+) Ready · (\d+) Later · (\d+) Far\.$` and compare all four
  against the totals; (2) for each category, parse its
  `^\*(\d+) done · (\d+) ready · (\d+) rows\.\*$` line and compare the three
  numbers against that category's tally. Emit one violation per mismatch,
  naming the category and both figures — e.g.
  `"ENGINE_MAP category 5: subtotal claims 17 done, tables have 18"`. A missing
  header or subtotal line is itself a violation. Fold the counts into the
  existing `$mapSummary` so the pass line reports them.
- **Proof:** `pwsh -NoProfile -File tools/check-invariants.ps1` still prints
  "all 13 checks passed" and `map-dependencies` reports the verified totals.
  Then change `76 Done` to `75 Done` in `docs/ENGINE_MAP.md`, re-run, and
  confirm the check fails naming that mismatch; revert and confirm it passes
  again. Also confirm the script still passes under Windows PowerShell 5.1
  (`powershell -NoProfile -File tools/check-invariants.ps1`).

---

## Needs a decision — not executed

These came out of the audit but failed the admission test. Listed so they are
not lost; `/analizeMax-execute` will not touch them. Ordered by consequence.

### C1 — The scene-file and prefab subsystem is reachable only from gates
- **Failed:** test 3 and the "never in scope: new features" rule — wiring a
  load path adds an API and changes what the program can do.
- **The decision:** `read_world(std::string_view text, World&)` has no
  path-taking counterpart, no runtime caller, and no `.solscene` exists in the
  repository. Do you add `load_world(IAssetLoader&, virtual_path, World&)`, ship
  a real `.solscene` under `packages/sandbox/content/`, and switch the sandbox's
  procedural 64-instance build over to it — or leave scene files as a library
  and record in ARCHITECTURE.md that they are not yet a content path? This is
  the most consequential finding in the report either way.

### D1 — 109 of 142 files disagree with the shipped `.clang-format`
- **Failed:** test 4 — three reasonable engineers would pick three different
  answers, and test 3 for one of them.
- **The decision:** 2,883 violation sites, no CI check, and `.editorconfig:15`
  asserting the two are "in agreement". The violations are almost entirely the
  author's deliberate column alignment, which LLVM style undoes. Three options:
  (a) reformat the tree and accept a 2,883-site diff; (b) relax
  `.clang-format` to permit the existing style —
  `AlignConsecutiveAssignments`, `AlignTrailingComments`,
  `AllowShortCaseLabelsOnASingleLine` — and reformat only the residue;
  (c) delete `.clang-format` and the `.editorconfig` claim, and let the style be
  conventional rather than enforced. Whichever is chosen, add a CI job, or the
  gap reopens.

### P1 — `ENGINE_RHI_D3D12=OFF` configures but does not compile
- **Failed:** test 4 — needs a decision about what a sandbox without a GPU
  backend should *be*; and test 3, since it changes what compiles.
- **The decision:** `packages/sandbox/src/main.cpp:31,55,56` include the WIC and
  DXC headers unguarded, and call into them at `:4607`, `:5372`, `:5733`. The
  CMake already emits `ENGINE_HAS_D3D12` and guards the linking. Do you guard
  the includes and give the sandbox a no-renderer path (headless gates only?
  early exit with a clear message?), or accept that the sandbox requires D3D12
  and say so in README's CMake options table? A *build* job for
  `ENGINE_RHI_D3D12=OFF` follows from whichever answer, and is step one of the
  stated Linux/macOS goal.

### S1 — Four content parsers refuse a file and log nothing
- **Failed:** test 4 — the convention is the decision.
- **The decision:** `cooked.cpp` (39 `return false`, 0 logs), `scene_file.cpp`
  (22/0), `pak.cpp` (21/0) and `prefab.cpp` (8/0) against siblings that all log
  — `mesh_loader_gltf.cpp` (7 logs), `image_loader_png.cpp` (5), `cvar.cpp` (6).
  Do these parsers log directly (adds no package edge — `scene` and `assets`
  both already link `core`), or return a structured error the caller reports?
  The first is cheaper and matches the tree; the second keeps parsing pure. Pick
  one and apply it to all four. Pairs naturally with C1, since a scene loaded
  from disk is the case where silence actually costs someone a day.

### A1 — The pass-addition path is eight files and three hand-written copy blocks
- **Failed:** "never in scope: refactors" — and test 4.
- **The decision:** a `I<Thing>Pipeline*` field must be threaded through
  `ForwardDemo` → `WorldExtractAssets` → `ExtractDesc` → `RenderSnapshot`, and
  `.claude/rules/renderer-boundaries.md` states that a missed copy line fails
  **silently**, because every `should_execute` predicate reads a null pipeline as
  "feature disabled". Do you replace the four structs with a pipeline registry
  keyed by name, so adding a pass is one insertion? This is the single criterion
  keeping Architecture out of the Exemplary band.

### S2 — `scene::add_instance` aborts on overflow, and it is a public API
- **Failed:** tests 3 and 4 — an API-shape change.
- **The decision:** `world.cpp:12,20` assert on `kMaxMaterials`/`kMaxInstances`,
  and asserts are live in Release. `docs/FOUNDATION.md:45` reserves asserts for
  "a violated precondition **inside the engine**" — a game's spawn loop reaching
  513 instances is outside it. Every engine caller bound-checks first, so nothing
  is broken today. Should these return `kInvalidInstance` and log instead, or
  should FOUNDATION.md's rule be narrowed to say that `scene`'s add functions are
  a programmer contract a game must bound-check itself?

### C2 — `content.pak` is installed and read by no runtime path
- **Failed:** test 4 — two defensible answers.
- **The decision:** the 16.4 MiB pak is copied next to both binaries and
  installed into the player layout, while the runtime creates only a filesystem
  loader (`main.cpp:5657`); `create_pak_loader` appears only in gates. Not
  dead — `run_pack_gate` (`main.cpp:2300-2319`) validates the shipped pak — and
  deliberate per `ROADMAP.md:244` ("Do not (still): … loading the forward pass
  from SOLC"). But the player download carries the 2048² albedo twice. Stop
  installing it until it is mounted, or mount it and stop installing loose
  content?

### A4 — `main.cpp` is 5,811 lines, 26% of the engine
- **Failed:** test 4, and it contradicts a documented rule.
- **The decision:** 68 gates, every pipeline description, the demo world, input
  handling and the exception boundary in one translation unit — 2.3× the next
  largest file. CLAUDE.md currently *defines* a gate as "a plain function in
  `packages/sandbox/src/main.cpp`", so splitting the gates into per-domain
  translation units changes that rule too. Worth doing, but not without deciding
  the new convention first.

### T3 — The installed skills contradict the project's method
- **Failed:** test 4 — the wording is a judgement call.
- **The decision:** the SessionStart hook injects
  `superpowers:using-superpowers` ("even a 1% chance… YOU DO NOT HAVE A
  CHOICE"), and the installed set includes `test-driven-development` against a
  project with no test framework, plus `finishing-a-development-branch`,
  `using-git-worktrees`, `requesting-code-review` and `code-review:code-review`
  against "Trunk-based: commit directly to `main`. No branches, no PRs."
  CLAUDE.md names two superpowers skills as part of the loop and never maps or
  excludes the conflicting ones, so every session re-derives the resolution. Add
  a short mapping section to CLAUDE.md — gates in place of TDD, trunk in place
  of the branch skills — or leave it to the precedence rule?

### D6 — `run.bat` is documented nowhere
- **Failed:** test 4 — document it or delete it, both reasonable.
- **The decision:** an interactive launcher at the repo root (prompts for GPU
  debug and extra args, checks the binary exists, prints the exit code),
  referenced by no markdown file except a completed migration plan. Add it to
  README's run section, or remove it as superseded by the two documented
  commands?
