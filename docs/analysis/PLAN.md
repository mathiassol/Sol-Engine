---
run: 2026-09-02
commit: 6dd4ef115d79263080754709befd700bb1b1f6bf
tasks: 4
phases: 2
---

# Remediation plan

Generated by /aim-audit. Every task here passed the admission test: reversible,
verifiable, no behaviour change, no judgement call, bounded.
Execute with `/aim-fix`.

Four of twenty findings produced a task. That ratio is the expected one and not a
sign the audit found little: this tree's open problems are mostly data-model and
scope decisions, and a decision is not a task. The four below are all factual
corrections to text that contradicts the tree — the category that is always in
scope and never needs approval. Everything else is in **Needs a decision**.

Both phases touch comments and documentation only. No compiled code changes, so
the gate suite cannot regress; the invariant script is the real verification,
because it enforces the `.editorconfig` these hand-tuned files obey (100 columns,
LF, no trailing whitespace, final newline) and would fail on a careless edit.

## Phase 1 — Instructions an agent executes

First because these are the ones that change what the next session does. T1 in
particular is a command that fails when run, in a rules file that auto-loads for
all renderer and RHI work.

**Verify:** `pwsh -NoProfile -File tools/check-invariants.ps1` (17/17) and
`.\solengine.bat help` runs

### T1 — Write the launcher as `.\solengine.bat`, the form that actually runs
- **Files:** `.claude/rules/renderer-boundaries.md`, `solengine.bat`
- **Why:** The rules file names the launcher bare at lines 35, 52 and 54
  (`solengine gates-vk`, `solengine run-gpu`, `solengine run-vk`,
  `solengine gates-gpu`). Run that way it fails: `& solengine help` answers
  "The term 'solengine' is not recognized as a name of a cmdlet, function,
  script file, or executable program", because the launcher is a batch file at
  the repository root and the root is not on PATH. README.md:99 already
  documents the correct form. This is the same trap CLAUDE.md records for the
  `aim` wrapper — a non-interactive shell without the expected PATH entry — and
  the lesson was not carried across.
- **Do:** In `.claude/rules/renderer-boundaries.md`, change each of the four bare
  invocations at lines 35, 52 and 54 to `.\solengine.bat <command>`, matching
  README.md. Keep the surrounding prose and the backticks unchanged, and keep
  every line within 100 columns. Then in `solengine.bat`, correct the two help
  lines that print the bare form: line 388 (`solengine ^<command^> [args]`) and
  lines 426-428 (`solengine gates-gpu`, `solengine run --set ...`,
  `solengine check`), plus line 438 (`Build it:  solengine build`) and line 145
  (`Unknown command "%CMD%". Try: solengine help`) — the tool should not teach
  its own wrong invocation.
- **Proof:** `pwsh -NoProfile -File tools/check-invariants.ps1` prints
  `all 17 checks passed` (format-hygiene covers both the markdown and the batch
  file's CRLF endings), and
  `Select-String -Path .claude/rules/renderer-boundaries.md -Pattern '(?<!\.\\)solengine'`
  returns no match outside the `.\solengine.bat` form.

### T2 — Say that two GPU backends exist, in the line that orients an agent
- **Files:** `CLAUDE.md`
- **Why:** Line 3 reads "General-purpose C++20 game engine
  (Unity/Godot/Unreal category), Windows/D3D12 today." A second backend shipped
  on 2 Sep 2026: `packages/rhi-vulkan` exists, is 3,910 lines, passes the whole
  gate suite, and renders a live frame. `.claude/rules/renderer-boundaries.md:31`
  states the consequence an agent needs — "a contract change costs **two**
  implementations, and **two** gate runs" — and the graphics-api-isolation
  invariant now fences Vulkan exactly as it fences D3D12. The guide's later
  sections are correct; only this first orienting sentence is stale, and it is
  the one that would let a session conclude one backend needs updating when two
  do.
- **Do:** Change the "Windows/D3D12 today" clause so it names both backends and
  records that D3D12 is the shipped player backend — the same split
  `renderer-boundaries.md` already states. Do not restate the FIFO defect here;
  that belongs in the rules file, which has it. Keep the line within 100 columns
  and leave the rest of the paragraph, including the pointers to Philosophy.md
  and Scaffold.md, untouched.
- **Proof:** `pwsh -NoProfile -File tools/check-invariants.ps1` prints
  `all 17 checks passed` (doc-links and format-hygiene both cover CLAUDE.md),
  and `Select-String -Path CLAUDE.md -Pattern 'rhi-vulkan|Vulkan'` matches in the
  opening section.

## Phase 2 — Counts and justifications that no longer describe the tree

Second because nothing in Phase 1 depends on it and the blast radius is smaller:
these are comments only, read by people rather than executed by agents.

**Verify:** `pwsh -NoProfile -File tools/check-invariants.ps1` (17/17)

### D3 — Correct three drifted counts
- **Files:** `.github/workflows/ci.yml`, `packages/core/src/assert.cpp`
- **Why:** Three explanatory comments carry figures that were right when written.
  `ci.yml` says "36 of the 74 gates are classified Cpu in kGates"; the
  gate-registry invariant reports `88 gates, all declared and classified
  (38 Cpu, 50 Gpu)`. The same file's portability job says a source scan finds
  "7 of 142 files" touching a platform API; the current count is 10 of 147, still
  every one inside its own package. And `assert.cpp` states twice — at lines 22
  and 35 — that "76 assert sites are live in Release"; counted across
  `packages/`, there are 107 (rhi-d3d12 67, rhi-vulkan 18, core 12, scene 7,
  sandbox 1, renderer 1, platform-win32 1). The last one matters most: it is the
  count of live abort sites in a shipping build, and it is the figure that
  motivates the crash-dump write it sits next to.
- **Do:** In `.github/workflows/ci.yml`, update the headless-gate comment to
  "38 of the 88 gates" and the portability-job comment to "10 of 147 files". In
  `packages/core/src/assert.cpp`, change "76 assert sites" to "107 assert sites"
  at both line 22 and line 35. Comments only — do not touch the surrounding
  argument in any of the three, which is still correct.
- **Proof:** `pwsh -NoProfile -File tools/check-invariants.ps1` prints
  `all 17 checks passed`, and
  `Select-String -Path .github/workflows/ci.yml,packages/core/src/assert.cpp`
  `-Pattern '\b74\b|\b76\b|\b142\b'` returns no match on those three claims.

### D1 — Drop the CI comment that claims a gap the tree has closed
- **Files:** `.github/workflows/ci.yml`
- **Why:** The `configure-options` job explains why it only configures: "with a
  backend switched off the sandbox does not compile (its PNG includes are
  unguarded). That is a real gap, but a separate one." It is not a gap any more.
  `packages/sandbox/src/main.cpp:92` guards the PNG include with
  `#ifdef ENGINE_HAS_PNG`, a define `cmake/EngineRuntimeApp.cmake` sets only when
  the WIC package is present. The `linux` job in the same file already builds the
  sandbox with both backends off, and building it directly on Windows confirms it:
  `cmake --preset no-d3d12` then `cmake --build build-off --config Debug`
  completed with 0 errors and exit code 0.
- **Do:** Replace the stale justification with the accurate one — the job checks
  that the CMake graph stays coherent across the option matrix, which is the
  purpose the rest of the comment already gives and which is unaffected. Keep the
  paragraph recording what this job caught the first time (the `packages/shaders`
  dependency that failed at generate time), because that is still true and is why
  the job exists. Do **not** change what the job runs; extending it from
  configure to build is the separate decision recorded below.
- **Proof:** `pwsh -NoProfile -File tools/check-invariants.ps1` prints
  `all 17 checks passed`, and
  `Select-String -Path .github/workflows/ci.yml -Pattern 'does not compile'`
  returns no match.

## Needs a decision — not executed

Eight questions the audit could not answer for you. Each is the decision itself,
not a task: the fix is clear in every case, but choosing it changes behaviour, an
API, or what a word in the backlog means.

### A3 / C1 — Should a scene instance be a handle instead of an array index?
- **Failed:** test 3 (behaviour change) and test 5 (unbounded) — it changes the
  scene's public API and ripples through the four structs instance data is copied
  through, plus the motion history.
- **The decision:** The scene is append-only. There is no `remove_instance`,
  `destroy_instance` or `clear`, so nothing can be despawned; the only removal is
  replacing the whole `World`. Compacting the array as a workaround is unsound,
  because `motion.hpp:25` keys `prev_model[512]` by the instance index, so
  closing a hole silently repoints every surviving instance at another object's
  previous transform. `packages/physics` already solves this correctly with a
  handle carrying a generation and a live flag per slot. Do you want the scene to
  adopt that model — which is the single change that most widens what can be
  built on this engine — and if so, is it a design spec and a Ready row rather
  than a plan task?

### S1 — Should `intern_name` return a sentinel instead of aborting?
- **Failed:** test 3 — a call that currently aborts would start returning 0.
- **The decision:** `world.hpp:79` states the rule for full containers: return
  the invalid sentinel and log, because "how many instances a game spawns is a
  content outcome, not a programmer error". `intern_name`, declared four lines
  below, calls `ENGINE_ASSERT_MSG` instead, and `ENGINE_ASSERT` has no `NDEBUG`
  guard — so the shipped player aborts once 513 distinct names have ever been
  interned. Reachable by renaming, not by spawning. Should `intern_name` follow
  the rule its own header states, and should the gate that proves it be written
  first?

### S2 — Should an over-long name be rejected rather than shortened?
- **Failed:** test 3 — a call that currently succeeds with a truncated name would
  start failing.
- **The decision:** `intern_name` copies `min(name.size(), 31)` with no log, so
  two names differing only after the 31st character collide into one identifier
  and lookup returns the wrong instance. The content path is already safe —
  `scene_file.cpp:158` rejects an over-long name with a positioned parse error
  and `prefab.cpp:143` rejects when prefix plus name would overflow — so only
  direct API calls reach it. Reject and log to match the content path, or
  document the cap as part of the API contract?

### S4 — Should success-returning functions be `[[nodiscard]]`, and warnings fatal?
- **Failed:** test 4 (taste) — which functions get the attribute is a style call,
  and warnings-as-errors is a trade against toolchain upgrades.
- **The decision:** 18 public functions return a bool meaning success; 3
  declarations in the whole public surface are marked, all in `arena.hpp`.
  Warnings are on at `/W4` but not as errors. The discipline currently holds —
  no call site in the engine packages drops a result, and a clean rebuild of
  renderer, core and scene produced 0 warnings — so this is about locking in a
  property the tree already has. Mark all 18, mark only the ones with an output
  parameter, or leave it? And add `/WX` now that the count is zero?

### A1 — Who should own the development-tree marker?
- **Failed:** test 3 — it changes `EngineConfig` or the detection contract.
- **The decision:** `packages/engine/src/engine.cpp:44` decides it is running
  from a development tree by testing for `packages/sandbox/content/test.txt` —
  layer 5 naming a layer 6 application package, as a string, which is why the
  downward-dependencies invariant cannot see it. A game built on Sol that renames
  the sandbox loses development-tree content resolution. Should the application
  supply the marker through `EngineConfig`, or should the engine test for a
  generic marker it owns — and should the invariant grow a check for
  `packages/<name>` literals in lower layers?

### A2 — Should the scene-to-renderer extract move into an engine package?
- **Failed:** test 3 and test 4 — it changes the application-facing shape of the
  renderer, and there is more than one reasonable boundary.
- **The decision:** No engine package bridges the scene to the renderer; the
  bridge is `packages/sandbox/src/world_extract.cpp`, 160 lines of application
  code. Two `static_assert`s in that file are the only thing coupling
  `scene::kMaxPointLights` to `renderer::kMaxPointLights` and `kMaxInstances` to
  `motion::kHistorySlots`, and the file's own comment says that if they diverge
  "it is a buffer overrun with nothing to catch it". A game writing its own
  extract — which it must, since the engine ships none — inherits no guard. Move
  the shared constants into one header both packages include (cheap, removes the
  overrun risk), move the extract itself (larger, gives a game a path it does not
  have to write), or both?

### D2 — How should a release be stopped from shipping ungated GPU code?
- **Failed:** test 3 — it changes what CI and the release workflow do, and may
  mean new infrastructure.
- **The decision:** 50 of 88 gates are GPU gates that no hosted runner can
  execute, for reasons documented in three places and not in dispute.
  `release.yml` states the consequence itself: "a release is only as gate-verified
  as the last local `--gates` run on the commit it was built from. Run them before
  you tag" — with nothing enforcing it. A self-hosted runner with a real adapter,
  a tag-time check that the commit has a recorded local gate run, or accept it and
  keep the note?

### T3 — Is a feature "Done" when it works on one of two backends?
- **Failed:** test 4 — this is about what a word in the canonical backlog means,
  and the backlog is mirrored by the service and read by the map-dependencies
  invariant.
- **The decision:** `docs/ENGINE_MAP.md:107` marks "Fullscreen / borderless /
  vsync control on the interface" **Done**. Vsync control is not honoured on the
  Vulkan backend — it takes IMMEDIATE regardless and warns, and warns that
  rendering stops after one frame if IMMEDIATE is absent — while RHI #25 carries
  that defect as Ready. Both rows are individually accurate and jointly
  misleading: reading the Platform category alone tells you vsync works. Qualify
  the Done row with a clause naming the backend split, change what Done requires
  for a two-backend feature, or leave the two rows to be read together?

### A4 — What should the "eight files" figure in CLAUDE.md say?
- **Failed:** test 4 — whether that number is a floor or a typical cost is a
  judgement about what the guide is telling a contributor.
- **The decision:** CLAUDE.md says "a working pass spans eight files — four
  structs to plumb through, plus the shader, the pass registration, the recorder,
  and the gate", which reads as a structural minimum and is a fair description of
  one. The most recent renderer feature, Renderer #16, measured 23 distinct files
  across four commits, 19 of them code, spanning renderer, rhi-vulkan, scene,
  sandbox and the shader content — because `opacity`, one float, is declared in
  three parallel structs and hand-copied at two boundaries. Keep eight as the
  minimum and add the measured cost of a real feature beside it, replace it, or
  leave it?
